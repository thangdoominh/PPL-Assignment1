void thua_so_nguyen_to()
                    {
                        fflush(stdin);
                        int arr[MAX];
                        char str[2048];
                        printf("Input: ");
                        fprintf(file_o,"*********NEW TEST*********\n");
                        for(int m=0;m<=j;m++)
                        {
                            int k=2,temp;
                            fprintf(file_o,"%d=",arr[m]);
                            while (arr[m] != 1)
                            {
                                if (arr[m] % k == 0)
                                {
                                    if(arr[m]!=temp)
                                    fprintf(file_o,"*");
                                    fprintf(file_o,"%d",k);
                                    arr[m] = k;
                                }

                                else
                                    k+2;
                            }
                        }
                        fclose(file_o);

                    }