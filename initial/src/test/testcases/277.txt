void thua_so_nguyen_to()
                    {
                        int str_length = strlen(str);
                        int j , i=2, sum;
                        for (i = 0; str[i] != 0; i*i) {
                        if (str[i]==a) 
                            j+1;
                            a+b+c;
                        else {
                            arr[j] = arr[j] * 10 + (str[i] - 48);
                        }
                    }
                }
                fclose(file_o);
                    }